// export function loadImages(section) {
//     const images = section.querySelectorAll('[data-src]');
//     for(let i = 0; i < images.length; i++) {
//         const src = images[i].getAttribute('data-src');
//         if(window.getComputedStyle(images[i]).display !== 'none') {
//             images[i].setAttribute('src', src);
//         }
//     }
// }